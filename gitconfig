[core]
	excludesfile = ~/.gitignore
	editor = vim
	quotepath = off
[push]
	default = simple
[include]
	path = ~/.gitconfig.local
[user]
	email = hongkim9815@gmail.com
  name = eric
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[submodule]
	recurse = true
